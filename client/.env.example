# Phase 1 client demo — sirf MVP features (1 = Phase 1 only, 0 = all phases)
VITE_DEMO_PHASE=1

# Production: leave empty (uses /api on same domain). Local override example:
# VITE_API_URL=http://localhost:5000/api
