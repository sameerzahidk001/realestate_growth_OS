{
  "version": 2,
  "buildCommand": "cd server && npx prisma generate && npx prisma db push --skip-generate && node src/utils/seed.js && cd ../client && npm install && npm run build",
  "outputDirectory": "client/dist",
  "installCommand": "npm install && cd server && npm install && cd ../client && npm install",
  "framework": null,
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api" },
    { "source": "/(.*)", "destination": "/index.html" }
  ],
  "functions": {
    "api/index.js": {
      "maxDuration": 60,
      "memory": 1024,
      "includeFiles": "{server/prisma/**,server/node_modules/.prisma/**,server/node_modules/@prisma/**,node_modules/.prisma/**,node_modules/@prisma/**}"
    }
  }
}
